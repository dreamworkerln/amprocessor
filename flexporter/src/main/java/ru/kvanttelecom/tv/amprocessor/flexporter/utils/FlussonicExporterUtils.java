package ru.kvanttelecom.tv.amprocessor.flexporter.utils;

public class FlussonicExporterUtils {

//    /*
//    /**
//     * Extract StreamKey from Prometheus metric vector
//     */
//    public static StreamKey extractStreamKey(VectorResult vector) {
//        String hostname = vector.metric.get(prometheus_series.flussonic.tags.instance)
//            .split("\\.", 2)[0].toLowerCase();
//        String stream = vector.metric.get(prometheus_series.flussonic.tags.stream);
//        return new StreamKey(hostname, stream);
//    }
//
//    public static Map.Entry<StreamKey, Boolean> getStreamAlive(VectorResult vector) {
//        String hostname = vector.metric.get(prometheus_series.flussonic.tags.instance)
//            .split("\\.", 2)[0].toLowerCase();
//        String stream = vector.metric.get(prometheus_series.flussonic.tags.stream);
//        boolean alive = vector.metric.get(prometheus_series.flussonic.tags.stream);
//        return new StreamKey(hostname, stream);
//    }
    
}
